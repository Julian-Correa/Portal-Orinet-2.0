[build]
  base = "ISP-Portal"
  command = "npm run build"
  publish = "ISP-Portal/dist"
  functions = "ISP-Portal/netlify/functions"

[build.environment]
  VITE_PORTAL_API_BASE = "/api"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
